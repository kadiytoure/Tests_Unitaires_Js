class Calculette {
    calcul(a, b, operateur) {
            return eval(a + operateur + b);
            let calculette = new Calculette();
            expect(calculette.operateur).toBe(ab);
        }
        /*
            calcul(c, d, operateur) {
                return eval(c + operateur + d);
                let calculette = new Calculette();
                expect(calculette.operateur).toBe(cd);
            }
            */
}