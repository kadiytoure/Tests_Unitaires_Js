describe('class Calculette', () => {
    let calculette;
    beforeEach(() => {
        calculette = new Calculette();
    });

    it('should add 2 to 2', () => {
        expect(calculette.calcul)
    })
})